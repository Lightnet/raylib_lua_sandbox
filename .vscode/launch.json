{
  "version": "0.2.0",
  "configurations": [

    {
      "type": "lua-local",
      "request": "launch",
      "name": "Debug Lua Executable",
      "cwd": "${workspaceFolder}",
      "program": {
        "command": "${workspaceFolder}/bin/raylua_s.exe"
      },
      "args": ["main.lua"]
    }
  ]
}